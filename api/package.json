{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "local": "ts-node-dev -r dotenv/config src/server.ts",
    "build": "tsc",
    "start": "node -r dotenv/config dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-psql": "npx -r dotenv/config kanel"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "@types/node": "^14.14.32",
    "kanel": "^1.1.0",
    "prettier": "2.2.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
