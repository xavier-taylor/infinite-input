import { Typography } from '@mui/material';
import { styled } from '@mui/styles';

// TODO go through app and apply this wherever we have chinese text
export const ChineseT = styled(Typography)({
  fontFamily: `'Noto Serif SC', serif;`,
});
